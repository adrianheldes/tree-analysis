# envaire_tree_analysis
A small project for analyzing trees.

## Installation

Install the Python dependencies from `requirements.txt`:

```bash
pip install -r requirements.txt
```

## Usage

Run the main script (example):

```bash
python pipeline.py
```

